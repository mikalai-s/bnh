@using Bnh.Entities
@model IEnumerable<Brick>
<div id="wallScene">
    @foreach (var brick in Model.OrderBy(b => b.Order))
    {
        @Html.Brick(brick, true)
    }
</div>
<div id="brickPrototype">@Html.BrickPrototype()</div>
