@model Bnh.Entities.Wall
<div class='wall-wrapper' style='width:@Model.GetUiWidth()' entity-data='@Model.GetUiData()'>
    <div class='wall'>
        <div class='header'>
            <span class='title'>Wall</span>
            <a class='delete'>delete</a>
        </div>
        <div class='content'>
            @foreach (var brick in Model.Bricks.OrderBy(b => b.Order))
            {
                @Html.Partial("DesignBrick", brick);
            }
        </div>
    </div>
</div>