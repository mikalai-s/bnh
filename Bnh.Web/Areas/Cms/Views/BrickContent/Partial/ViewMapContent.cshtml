@model MapContent
@{
    this.GetScriptBundle().Include(new []
    {
        "~/Areas/Cms/Scripts/brick-content-map.js"
    });

    var mappable = ViewBag.GlobalModel as Bnh.Cms.Models.IMappable;
}

@if (mappable != null)
{ 
    <div class="map-canvas-wrapper" zoom="@Model.Zoom" gpsLocation="@mappable.GpsLocation" gpsBounds="@mappable.GpsBounds">
        <div id="mapCanvas" style="height: @(Model.Height)px"></div>
    </div>
}
else
{
    <div class="map-canvas-wrapper"  zoom="@Model.Zoom" gpsLocation="@Model.GpsLocation">
        <div id="mapCanvas" style="height: @(Model.Height)px"></div>
    </div>
}