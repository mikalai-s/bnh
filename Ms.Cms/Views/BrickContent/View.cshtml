@model BrickContent

@{
    string view = ContentUrl.Resolve("/Views/BrickContent/Partial/View{0}.cshtml").FormatWith(Model.GetType().Name);
}
@if (File.Exists(Server.MapPath(view)))
{ 
    @Html.Partial(view, Model);
}