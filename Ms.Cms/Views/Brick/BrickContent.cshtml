@using Ms.Cms.Models

@model Brick

@{
    var model = Model.EnsureNonLinked();
    string suffix =
        (model is RazorBrick) ? "Razor" :
        (model is HtmlBrick) ? "Html" :
        (model is MapBrick) ? "Map" :
        (model is TocBrick) ? "Toc" :
        null;
}
@if (!string.IsNullOrEmpty(suffix))
{ 
    @Html.Partial("~/WebExtracted/Ms.Cms/Views/Brick/BrickContent_" + suffix + ".cshtml", model);
}